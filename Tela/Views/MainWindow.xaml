<Window x:Class="Backup_Restore.Views.MainWindow"
        Loaded="Window_Loaded"
        TextElement.FontFamily="Segoe UI"
        TextElement.Foreground="#333333"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Backup_Restore.Views"
        mc:Ignorable="d"
        Title="HOS - Backup Restore" Height="600" Width="450"
        Icon="/Resource/hos-farma.png"
        Closing="Window_Closing">
    <Window.Background>
        <ImageBrush ImageSource="/Resource/dromi-hos.png" Opacity="1"  />
    </Window.Background>
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#315C85"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                            CornerRadius="5" 
                            Padding="10,5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>

            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#447AAB"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Height" Value="28"/>
            <Setter Property="Padding" Value="5,0,0,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border CornerRadius="4" 
                        BorderBrush="{TemplateBinding BorderBrush}" 
                        BorderThickness="{TemplateBinding BorderThickness}" 
                        Background="{TemplateBinding Background}">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>

            <Style.Triggers>
                <Trigger Property="IsKeyboardFocused" Value="True">
                    <Setter Property="BorderBrush" Value="#315C85"/>
                    <Setter Property="BorderThickness" Value="2"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="TabItem">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="Border" 
                        Background="Transparent" 
                        BorderBrush="#315C85" 
                        BorderThickness="0,0,0,2" 
                        Margin="5,0,5,0" 
                        Padding="15,8" 
                        CornerRadius="5,5,0,0">

                            <ContentPresenter x:Name="ContentSite"
                                      VerticalAlignment="Center"
                                      HorizontalAlignment="Center"
                                      ContentSource="Header"
                                      RecognizesAccessKey="True"
                                      TextElement.Foreground="#315C85"/>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#315C85"/>
                                <Setter TargetName="Border" Property="BorderThickness" Value="0"/>
                                <Setter TargetName="ContentSite" Property="TextElement.Foreground" Value="White"/>
                            </Trigger>

                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#E0EBF5"/>
                            </Trigger>

                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsSelected" Value="True"/>
                                    <Condition Property="IsMouseOver" Value="True"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="Border" Property="Background" Value="#315C85"/>
                                <Setter TargetName="ContentSite" Property="TextElement.Foreground" Value="White"/>
                            </MultiTrigger>
                        </ControlTemplate.Triggers>

                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <TabControl x:Name="AbasMenu" Background="Transparent" BorderThickness="0" Margin="0,10,0,0">
        <TabItem Header="Configurações" Background="Transparent">
            <Grid Margin="15" Background="Transparent">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="0,0,0,10" Width="400">
                        <StackPanel Name="painelAgendamento" Visibility="Collapsed" Margin="0,15,0,5" Width="380" HorizontalAlignment="Center">
                            <StackPanel>
                                <TextBlock Text="Detalhes do Agendamento" FontWeight="SemiBold" Foreground="#315C85" Margin="0,0,0,12" HorizontalAlignment="Center"/>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="15"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="Data de Início:" Margin="0,0,0,5" FontWeight="SemiBold"/>
                                        <DatePicker x:Name="dtpAgendaManutencao" Height="28" Margin="0,0,0,10" Width="180" HorizontalAlignment="Left"/>
                                    </StackPanel>

                                    <StackPanel Grid.Column="2">
                                        <TextBlock Text="Horário de Execução:" Margin="0,0,0,5" FontWeight="SemiBold"/>
                                        <StackPanel Orientation="Horizontal">
                                            <ComboBox x:Name="cmbHora" Width="55" Height="28" HorizontalContentAlignment="Center"/>
                                            <TextBlock Text=":" FontWeight="Bold" FontSize="16" VerticalAlignment="Center" Margin="3,0"/>
                                            <ComboBox x:Name="cmbMinuto" Width="55" Height="28" HorizontalContentAlignment="Center"/>
                                        </StackPanel>
                                    </StackPanel>
                                </Grid>
                            </StackPanel>
                        </StackPanel>
                        <TextBlock Text="Banco de Dados:" HorizontalAlignment="Left" Margin="0,13,0,5" FontWeight="SemiBold"/>
                        <ComboBox Name="cmbBanco" SelectionChanged="cmbBanco_SelectionChanged" Margin="0,0,20,10" Height="25">
                            <ComboBoxItem Content="Firebird 2.5"/>
                            <ComboBoxItem Content="Firebird 4.0"/>
                            <ComboBoxItem Content="PostgresSQL"/>
                        </ComboBox>

                        <TextBlock Name="lblAcao" Text="O que deseja fazer?" HorizontalAlignment="Left" Margin="0,0,0,5" FontWeight="SemiBold" Visibility="Collapsed" />
                        <ComboBox Name="cmbAcao" SelectionChanged="cmbAcao_SelectionChanged" Margin="0,0,20,10" Height="25" Visibility="Collapsed">
                            <ComboBoxItem Content="Backup"/>
                            <ComboBoxItem Content="Restore"/>
                            <ComboBoxItem Name="cbiManutencao" Content="Manutenção" Visibility="Collapsed"/>
                        </ComboBox>

                        <TextBlock Name="lblOrigem" Text="Caminho do Banco Origem:" Margin="0,4,0,0" HorizontalAlignment="Left" FontWeight="SemiBold" />
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                            <TextBox Name="txtOrigemPath" Height="25" Margin="0,5,8,0" Width="280" />
                            <Button Name="btnBuscarOrigem" Content="Procurar..." Height="25" Margin="0,5,0,0" Width="95" HorizontalAlignment="Left" Click="btnBuscarOrigem_Click" />
                        </StackPanel>

                        <TextBlock Name="lblDestino" Text="Caminho Destino do Banco:" Margin="0,4,0,0" HorizontalAlignment="Left" FontWeight="SemiBold" />
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                            <TextBox Name="txtDestinoPath" Height="25" Margin="0,5,8,0" Width="280" SnapsToDevicePixels="True"/>
                            <Button Name="btnBuscarDestino" Content="Procurar..." Height="25" Margin="0,5,0,0" Width="95" HorizontalAlignment="Left" Click="btnBuscarDestino_Click" />
                        </StackPanel>

                        <TextBlock Name="lblVersao" Text="Caminho Versão Postgres:" Margin="0,4,0,0" HorizontalAlignment="Left" FontWeight="SemiBold" Visibility="Collapsed" />
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                            <TextBox Name="txtVersao" Height="25" Margin="0,5,8,0" Width="280" SnapsToDevicePixels="True" Visibility="Collapsed"/>
                            <Button Name="btnBuscarVersao" Content="Procurar..." Height="25" Margin="0,5,0,0" Width="95" HorizontalAlignment="Left" Click="btnBuscarVersao_Click" Visibility="Collapsed"/>
                        </StackPanel>

                        <TextBlock Name="lblNomeBanco" Text="Nome do Banco:" Margin="0,10,0,0" HorizontalAlignment="Left" FontWeight="SemiBold" Visibility="Collapsed" />
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                            <TextBox Name="txtNomeBanco" Height="25" Margin="0,5,0,0" Width="380" SnapsToDevicePixels="True" Visibility="Collapsed"/>
                        </StackPanel>

                        <StackPanel Margin="0,5" HorizontalAlignment="Left" Width="378">
                            <TextBlock Name="lblSenha" Text="Senha Banco Postgres:" HorizontalAlignment="Left" Margin="0,0,0,5" FontWeight="SemiBold" Visibility="Collapsed"/>
                            <DockPanel Name="panelSenha" Visibility="Collapsed">
                                <ToggleButton Name="btnVerSenha" Content="👁" DockPanel.Dock="Right" Margin="5,0,0,0" Width="35" Click="btnVerSenha_Click" ToolTip="Ver/Ocultar Senha" SnapsToDevicePixels="False" RenderOptions.BitmapScalingMode="HighQuality" RenderOptions.EdgeMode="Aliased"/>
                                <Border CornerRadius="5" BorderBrush="#CCCCCC" BorderThickness="1" Height="25">
                                    <Grid>
                                        <PasswordBox Name="pbSenha" BorderThickness="0" Background="Transparent" Padding="5,0,0,0" VerticalContentAlignment="Center" PasswordChar="*"/>
                                        <TextBox Name="txtSenhaVisivel" BorderThickness="0" Background="Transparent" Padding="5,0,0,0" VerticalContentAlignment="Center" Visibility="Collapsed"/>
                                    </Grid>
                                </Border>
                            </DockPanel>
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>

                <Grid Grid.Row="1" Margin="0,15,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Button Name="btnIniciar" Grid.Column="0" Content="Iniciar" Click="btnIniciar_Click" />
                    <Button Name="btnAgendar" Grid.Column="1" Content="Agendar" Margin="5,0,5,0" Click="btnAgendar_Click" />
                    <Button Name="btnSair" Grid.Column="2" Content="Sair" Click="btnSair_Click" />
                </Grid>

            </Grid>
        </TabItem>
        <TabItem Header="Monitor">
            <Grid Margin="20">
                <TextBox x:Name="txtTerminal" 
                 Height="Auto" 
                 VerticalAlignment="Stretch" 
                 HorizontalAlignment="Stretch"
                 VerticalContentAlignment="Top"
                 Background="#1E1E1E" 
                 Foreground="#4AF626" 
                 FontFamily="Consolas" 
                 FontSize="14" 
                 TextWrapping="Wrap" 
                 AcceptsReturn="True" 
                 VerticalScrollBarVisibility="Auto" 
                 IsReadOnly="True"
                 Padding="10"
                 BorderThickness="0"
                 Text="Aguardando inicialização do processo...&#x0A;"/>
            </Grid>
        </TabItem>
    </TabControl>
</Window>